apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: catalog-nginx-ingress
spec:
  ingressClassName: nginx
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: catalog-service
            port:
              number: 8080
