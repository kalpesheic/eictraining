apiVersion: v1
kind: Pod
metadata:
  name: s3-test
  namespace: production
spec:
  serviceAccountName: s3-sa

  containers:
  - name: aws-cli
    image: amazon/aws-cli
    command:
    - sleep
    - "3600"
