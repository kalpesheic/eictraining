#Dictionary in python
Dictionary are used to store data values in key:value pairs
They are unordered, mutable(Changed) and don't allow duplicate keys

# Netsted dictionary
Creating dictonaty inside dictionary would call nested dictionary
